package com.demo.tools.impl;

import com.demo.model.ToolResult;
import com.demo.tools.RunPythonTool;

/**
 * Abstract implementation for running Python code/scripts. Extends RunPythonTool
 * and inherits the execute() contract from it. This class remains abstract to
 * allow concrete implementations to provide execution details.
 */
public abstract class RunPythonToolImpl extends RunPythonTool {
    // execute() remains abstract as part of the template; subclasses must implement
}
